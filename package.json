{
  "name": "designbook",
  "version": "0.1.0",
  "description": "Local-first AI design workbench on the frontendmaxxing vault — deterministic drafts, dual Claude engines (Claude Code via MCP / Agent SDK), multi-viewport layout facts instead of screenshots.",
  "type": "module",
  "main": "server.js",
  "bin": {
    "designbook": "./server.js"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node test.js",
    "mcp": "node designbook-mcp/server.js",
    "setup:pptx": "python3 -m venv .pptx-venv && ./.pptx-venv/bin/pip install -q python-pptx"
  },
  "optionalDependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.5.0"
  },
  "license": "MIT",
  "dependencies": {
    "culori": "^4.0.2"
  }
}
